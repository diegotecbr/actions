name: Java Example
on:
  push:
    branches:
    - release/**
jobs:
  test-java-version:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Java JDK
        uses: actions/setup-java@v3.4.1
        with:
          distribution: 'zulu'
          java-version: '11'
        

